.page-header
  = link_to math_ladders_path, :class => 'btn btn-default' do
    %span.glyphicon.glyphicon-list-alt
    Back
  = link_to @math_ladder, :class => 'btn btn-primary' do
    %span.glyphicon.glyphicon-info-sign
    Show
= render 'title'

= render 'form'
